name: Diagnose bouclier meta (temporary)

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/report-bouclier-2026
      - name: Print bouclier metadata
        run: python scripts/_print_bouclier_meta.py
