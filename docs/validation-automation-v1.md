# Validation de la branche automation-v1

Cette branche est une branche de test. Aucun changement n'est publié sur `main` avant fusion explicite de la pull request.

Le `data.json` de test est généré par le même pipeline append-only que le workflow hebdomadaire et sert à valider l'intégration complète avant mise en production.
